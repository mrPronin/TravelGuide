<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/start"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="8dip" >
	
    <Button android:id="@+id/StartButton"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:text="@string/start_title" android:background="@drawable/button_style"/>

    <Button
        android:id="@+id/AboutButton"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:textColor="@android:color/black"
        android:text="@string/about_title"
        android:textStyle="bold" android:background="@drawable/button_style"/>

    <Button android:id="@+id/ExitButton"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:text="@string/exit_title" android:background="@drawable/button_style"/>
</LinearLayout>